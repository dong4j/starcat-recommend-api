# Deploy to Render

Use the Dockerfile at repository root.

Environment variables:

- `PORT=5005`
- `API_KEYS`
- `SIMREPO_API_KEY`
- `SIMREPO_ENDPOINT` optional

Health check path:

```text
/healthz
```
